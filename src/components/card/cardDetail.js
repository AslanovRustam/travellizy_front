export default function CardDetail({
  name,
  display,
  url,
  OS,
  RAM,
  storageCapacity,
  graphicsCard,
  corps,
  weight,
  battery,
  dimensions,
  connection,
  producingCountry,
  additionalFeatures,
  registration,
  guarantee,
  price,
  quantity,
}) {
  return (
    <a target="_blank" href="{}">
      {/* <img src="{}" alt="{}" width="244px" height="280px" />
      <div>
        <span>{}</span>
        <span>{} </span>
      </div> */}
      HELLO
    </a>
  );
}
